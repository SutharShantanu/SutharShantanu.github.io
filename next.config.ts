import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["cdn-icons-png.flaticon.com", "seeklogo.com", "img.icons8.com", "cdn-1.webcatalog.io", "images.g2crowd.com", "images.crunchbase.com"],
  },
};

export default nextConfig;
